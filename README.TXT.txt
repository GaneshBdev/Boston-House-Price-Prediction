Machine Learning Final project:

The file is saved in Jupyter Notebook File (.ipynb).
Run the file in 'JUPYTER NOTEBOOK' to look at the result, and the dataset is taken from the credit_card_information.csv which is inside the folder

The data set is gathered from Kaggle database.

